name: Schedule smoke test

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions: { }

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - name: Log execution
        run: echo "schedule-smoke-test event=${{ github.event_name }} at=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
